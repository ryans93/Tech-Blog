{{> nav}}
{{#each posts as |post|}}
 <div class="card">
  <div class="container">
    <h3>{{post.title}}</h3>
    <h5>Posted by {{post.user.username}} on {{formatDate post.date_created}} at {{formatTime post.date_created}}</h5>
    <p>{{post.content}}</p>
  </div>
</div> 
{{/each}}